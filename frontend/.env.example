# Base URL of the backend API. The frontend falls back to http://localhost:8000/api/v1
# when VITE_API_URL is not set. This value is baked into the client at build time,
# so changes require a rebuild (or restart of `npm run dev`).
VITE_API_URL=http://localhost:8000/api/v1
