# Taste
- Uses `uv` (via `uv run ...`) to run Python tooling and dev servers (e.g., `uv run uvicorn app.main:app --reload`) instead of manually activating a virtualenv or invoking `python -m` directly. Confidence: 0.8
- Wants Python package installations done inside the project's virtual environment (e.g., `uv pip install` targeting the venv), never the global Python — explicitly asked to "ensure python installations are done in the virtual environment accordingly." Confidence: 0.8
- Prefers applications to be fully local / offline-first by default, with cloud or API-based features optional — explicitly motivated by supporting low-end devices (e.g., small quantized CPU models, no required external services). Explicitly confirmed they still want Ollama and cloud providers (via API keys) fully supported in parallel as options, not removed. Confidence: 0.9
- Prefers extending the existing architecture over removing or rewriting working parts; changes should keep the system deployable to the web later (backward-compatible, additive). Confidence: 0.8
- Prefers to run package/dependency installations AND start the server himself rather than having the agent execute them — explicitly said "run code I will make installations necessary" and "dont start server to test anything. I will start server on my own"; expects the agent to write the code, document install/run steps, and provide a self-verification checklist (what to check, endpoints to hit) instead of testing live endpoints. Confidence: 0.95
- Communicates by pasting raw terminal/error output (e.g., npm, Vite, uvicorn errors) with minimal prose and expects the agent to diagnose the root cause and apply the fix directly (editing configs/dependencies), rather than asking clarifying questions first. Confidence: 0.5
- For local inference, prefers a lightweight in-process, pip-installable runtime (llama-cpp-python with small quantized GGUF models) over heavier stacks (torch/sentence-transformers) or separate-server requirements (Ollama) — minimizing setup, RAM, and CPU burden for low-end devices; confirmed this choice when offered alternatives. Confidence: 0.7
- Wants a suggestion/plan presented before large architectural changes are implemented, rather than immediate implementation; also prefers to be asked for explicit options on key decisions (e.g., local engine, default model, whether to install packages) and will choose accordingly rather than having the agent decide unilaterally. Confidence: 0.8
- Wants the task/todo list kept current as work proceeds — explicitly asks to update the todo list first before continuing implementation. Confidence: 0.6
