from typing import List, Optional


class GraphService:
    def build_graph(self, document_ids: List[str], depth: int = 2, project_id: Optional[str] = None):
        raise NotImplementedError("Knowledge graph is planned for Phase 3")
